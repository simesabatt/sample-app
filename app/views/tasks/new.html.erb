<% provide(:title, 'New Task') %>

<h1>タスク新規作成</h1>

<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <%= form_with(model: @task, url: user_tasks_path(@user), local:true) do |f| %>
    
      <%= f.label :task_title, "名称", class: "label-form" %>
        <%= f.text_field :task_title, class: "form-control" %>
      
      <%= f.label :task_detail, "詳細", class: "label-form" %>
        <%= f.text_area :task_detail, rows: 5, class: "form-control" %>
      
      <%= f.hidden_field :user_id, value: @user %>
      
      <%= f.submit "新規登録", class: "btn btn-primary btn-form" %>
      
    <% end %>
  </div>
</div>